# scripts

- Purpose: Windows-friendly helper scripts for local hub-api workflows.
- Responsibilities: Start the API, reset local state, run the development seed, and sync the default admin from `.env` in a predictable way.
- Interfaces: Wraps `python -m` commands and prefers the local `.alice` interpreter when present.
- Status: in progress.
